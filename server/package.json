{
  "name": "invos-mock-demo-server",
  "version": "1.0.0",
  "description": "Fastify server for ingesting mock Taiwanese e-invoice data into PostgreSQL.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "replay": "node scripts/replay.js",
    "test": "node --test"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "fastify": "^5.2.0",
    "pg": "^8.13.1",
    "prom-client": "^15.1.3"
  }
}
